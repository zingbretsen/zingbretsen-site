import Layout from '@components/layout'

export const meta = {
author: 'Zach Ingbretsen',
path: '/blog/fzf',
date: '2021-11-24',
title: 'Using fzf for Fun and Profit',
tags: ['quick-byte', 'fzf', 'linux']
}

A new favorite tool of mine is `fzf`, which is a `f`u`z`zy `f`inder. It's very simple, in that you pipe in some text and `fzf` allows you to to find the lines that match whatever pattern you type. The power is how it can be combined with other tools.

## asdf

The sky is the limit! Let me know if you use `fzf` in any particularly fun ways.

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
